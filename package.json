{
  "name": "cards",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@canonical/react-components": "^0.47.0",
    "@testing-library/jest-dom": "^5.17.0",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "axios": "^1.5.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^3.0.1",
    "sass": "^1.69.3",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start ",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "build-css": "sass -w --load-path=node_modules src/style.scss:src/style.css --style=compressed",
    "dev": "concurrently \"npm run start\" \"npm run build-css\""
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "concurrently": "^8.2.1"
  }
}
